examResult(10);
function examResult(note) { //quando é função colocar dentro de parentese como a seguir examResult();
    if (note >= 75) {
        console.log('You passed the exam!');
    } else {
        console.log('You didnt passed the exam!');
        
    }
    
}
